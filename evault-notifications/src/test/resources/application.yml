spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  mail:
    host: localhost
    port: 3025
    username: test@evault.gov.in
    password: test-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

evault:
  mail:
    from: noreply@evault.gov.in
